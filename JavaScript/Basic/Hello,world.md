## <script> 태그
  `<script>` 태그 사이에 자바스크립트 코드 삽입 가능  
```
  <script>
    alert("Hello,world!");
  </script>
  ```
  
  ## 외부 스크립트
  src 속성(attribute)을 사용하여 HTML에 삽입 가능  
  ```
  <scritp src="/path/to/script.js">
  </script>
  ```  
  *scr 속성이 있을 경우 내부의 코드는 무시된다
